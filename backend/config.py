# Define connection string (one only) mongodb://<user>:<password>@<mongodbURL>
host = "mongodb+srv://seproject:kevin@cluster0.bj6up.mongodb.net/myFirstDatabase?retryWrites=true&w=majority"

# Select Database (one only)
db = "SEProject"

# Define collections (can be multiple)
memberCol = "member"
recordCol = "record"
recordCategoryCol = "recordCategory"
budgetCol = "budget"
accountCol = "account"